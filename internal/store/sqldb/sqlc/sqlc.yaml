version: "2"
sql:
  - engine: "postgresql"
    schema: "../migrations"
    queries: "queries"
    gen:
      go:
        out: .
        package: sqlc
        emit_interface: true
        emit_exported_queries: true
