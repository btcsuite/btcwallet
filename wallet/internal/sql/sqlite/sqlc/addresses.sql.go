// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: addresses.sql

package sqlc

import (
	"context"
	"database/sql"
)

const CreateAddress = `-- name: CreateAddress :exec
INSERT INTO addresses (
    wallet_id,
    scope_id,
    address_hash,
    account_number,
    address_type,
    added_at,
    sync_status,
    branch,
    address_index,
    encrypted_pub_key,
    encrypted_priv_key,
    encrypted_hash,
    encrypted_script,
    witness_version,
    is_secret_script,
    used
) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
`

type CreateAddressParams struct {
	WalletID         int64
	ScopeID          int64
	AddressHash      []byte
	AccountNumber    int64
	AddressType      int64
	AddedAt          int64
	SyncStatus       int64
	Branch           sql.NullInt64
	AddressIndex     sql.NullInt64
	EncryptedPubKey  []byte
	EncryptedPrivKey []byte
	EncryptedHash    []byte
	EncryptedScript  []byte
	WitnessVersion   sql.NullInt64
	IsSecretScript   sql.NullBool
	Used             bool
}

func (q *Queries) CreateAddress(ctx context.Context, arg CreateAddressParams) error {
	_, err := q.exec(ctx, q.createAddressStmt, CreateAddress,
		arg.WalletID,
		arg.ScopeID,
		arg.AddressHash,
		arg.AccountNumber,
		arg.AddressType,
		arg.AddedAt,
		arg.SyncStatus,
		arg.Branch,
		arg.AddressIndex,
		arg.EncryptedPubKey,
		arg.EncryptedPrivKey,
		arg.EncryptedHash,
		arg.EncryptedScript,
		arg.WitnessVersion,
		arg.IsSecretScript,
		arg.Used,
	)
	return err
}

const GetAddress = `-- name: GetAddress :one
SELECT
    wallet_id,
    scope_id,
    address_hash,
    account_number,
    address_type,
    added_at,
    sync_status,
    branch,
    address_index,
    encrypted_pub_key,
    encrypted_priv_key,
    encrypted_hash,
    encrypted_script,
    witness_version,
    is_secret_script,
    used
FROM addresses
WHERE wallet_id = ? AND scope_id = ? AND address_hash = ?
`

type GetAddressParams struct {
	WalletID    int64
	ScopeID     int64
	AddressHash []byte
}

func (q *Queries) GetAddress(ctx context.Context, arg GetAddressParams) (Address, error) {
	row := q.queryRow(ctx, q.getAddressStmt, GetAddress, arg.WalletID, arg.ScopeID, arg.AddressHash)
	var i Address
	err := row.Scan(
		&i.WalletID,
		&i.ScopeID,
		&i.AddressHash,
		&i.AccountNumber,
		&i.AddressType,
		&i.AddedAt,
		&i.SyncStatus,
		&i.Branch,
		&i.AddressIndex,
		&i.EncryptedPubKey,
		&i.EncryptedPrivKey,
		&i.EncryptedHash,
		&i.EncryptedScript,
		&i.WitnessVersion,
		&i.IsSecretScript,
		&i.Used,
	)
	return i, err
}

const ListAccountAddresses = `-- name: ListAccountAddresses :many
SELECT
    wallet_id,
    scope_id,
    address_hash,
    account_number,
    address_type,
    added_at,
    sync_status,
    branch,
    address_index,
    encrypted_pub_key,
    encrypted_priv_key,
    encrypted_hash,
    encrypted_script,
    witness_version,
    is_secret_script,
    used
FROM addresses
WHERE scope_id = ? AND account_number = ?
ORDER BY address_hash
`

type ListAccountAddressesParams struct {
	ScopeID       int64
	AccountNumber int64
}

func (q *Queries) ListAccountAddresses(ctx context.Context, arg ListAccountAddressesParams) ([]Address, error) {
	rows, err := q.query(ctx, q.listAccountAddressesStmt, ListAccountAddresses, arg.ScopeID, arg.AccountNumber)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Address
	for rows.Next() {
		var i Address
		if err := rows.Scan(
			&i.WalletID,
			&i.ScopeID,
			&i.AddressHash,
			&i.AccountNumber,
			&i.AddressType,
			&i.AddedAt,
			&i.SyncStatus,
			&i.Branch,
			&i.AddressIndex,
			&i.EncryptedPubKey,
			&i.EncryptedPrivKey,
			&i.EncryptedHash,
			&i.EncryptedScript,
			&i.WitnessVersion,
			&i.IsSecretScript,
			&i.Used,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const MarkAddressUsed = `-- name: MarkAddressUsed :execrows
UPDATE addresses
SET used = TRUE
WHERE wallet_id = ? AND scope_id = ? AND address_hash = ?
`

type MarkAddressUsedParams struct {
	WalletID    int64
	ScopeID     int64
	AddressHash []byte
}

func (q *Queries) MarkAddressUsed(ctx context.Context, arg MarkAddressUsedParams) (int64, error) {
	result, err := q.exec(ctx, q.markAddressUsedStmt, MarkAddressUsed, arg.WalletID, arg.ScopeID, arg.AddressHash)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected()
}
